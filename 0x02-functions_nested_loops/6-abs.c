#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @nbr: the int to check
 * Return: the absolute value of int number
 */
int _abs(int nbr)
{
	if (nbr >= 0)
	{
		return (nbr);
	}
	return (-nbr);
}
